title: AI Workbench
description: A per-bundle private AI harness for dev and QA collaboration. Turns Jira epics into PRDs, engineering specs, TDDs, BDDs, test cases, and ralph fix_plans across multiple code repos.
url: https://amit-t.github.io
baseurl: /ai-workbench

plugins:
  - jekyll-seo-tag

exclude:
  - README.md
  - Gemfile
  - Gemfile.lock

defaults:
  - scope:
      path: ""
    values:
      layout: default
